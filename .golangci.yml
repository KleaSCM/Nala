run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gofmt
    - revive
    - gosec

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
